// Curated CSS-variable palettes for this theme. Keep the option list short (per
// problem_statement.md: a handpicked palette, not a free color picker) so results stay professional.
export const palettes: Record<string, Record<string, string>> = {
  default: {
    "--bg": "#ffffff",
    "--fg": "#111111",
    "--accent": "#3b82f6",
    "--muted": "#6b7280",
    "--border": "#e5e7eb",
  },
};
